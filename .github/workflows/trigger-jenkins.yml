name: Trigger Jenkins

on:
push:
branches:
- main

jobs:
trigger-jenkins:

```
runs-on: ubuntu-latest

steps:

  - name: Trigger Jenkins Job
    run: |
      curl -X POST "http://localhost:8081//job/test/build?token=mytoken123"
```
